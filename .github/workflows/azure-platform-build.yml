name: Azure readiness - Platform Website

on:
  workflow_dispatch:
  push:
    paths:
      - "apps/retro-platform-web/**"
      - "packages/platform-contracts/**"
      - "packages/realtime-client/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/azure-platform-build.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      VITE_API_URL: ${{ vars.PUBLIC_API_URL }}
      VITE_AI_BOT_URL: ${{ vars.PUBLIC_AI_BOT_URL }}
      VITE_RETRO_RUSH_URL: ${{ vars.PUBLIC_RETRO_RUSH_URL }}
      VITE_SPIN_THE_BOTTLE_URL: ${{ vars.PUBLIC_SPIN_URL }}
      VITE_ROOM_SERVICE: real
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22.13.0
          cache: npm
      - name: Require public build configuration
        run: test -n "$VITE_API_URL" && test -n "$VITE_AI_BOT_URL" && test -n "$VITE_RETRO_RUSH_URL" && test -n "$VITE_SPIN_THE_BOTTLE_URL"
      - run: npm ci
      - run: npm run build:web
      - uses: actions/upload-artifact@v7
        with:
          name: platform-static-web-app
          path: apps/retro-platform-web/dist


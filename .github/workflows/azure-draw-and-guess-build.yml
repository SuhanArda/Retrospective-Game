name: Azure readiness - Draw and Guess

on:
  workflow_dispatch:
  push:
    paths:
      - "games/draw-and-guess/**"
      - "packages/platform-contracts/**"
      - "packages/realtime-client/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/azure-draw-and-guess-build.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # Readiness-only values. A deployment workflow must map real Azure URLs.
      VITE_API_URL: https://api.example.invalid
      VITE_PLATFORM_URL: https://platform.example.invalid
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22.13.0
          cache: npm
      # This game falls back to localhost when a URL is absent instead of
      # failing the build, so the guard has to be explicit here.
      - name: Require public build configuration
        run: test -n "$VITE_API_URL" && test -n "$VITE_PLATFORM_URL"
      - run: npm ci
      - run: npm run test:draw-and-guess
      - run: npm run build:draw-and-guess
      - uses: actions/upload-artifact@v7
        with:
          name: draw-and-guess-static-web-app
          path: games/draw-and-guess/dist

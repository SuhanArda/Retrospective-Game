# Shared packages

This directory is reserved for deliberately shared frontend components, TypeScript DTOs, transport contracts, themes, and game utilities. Existing game code should remain game-local until a stable multi-consumer abstraction is identified.
